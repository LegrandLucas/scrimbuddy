<div class="sb-container">
  <div class="row">
    <div class="col-4">
      <%= render 'shared/player_card_show_player' %>
    </div>
    <div class="col-8">
      <div class="stat-category">
        Personnal statistics
      </div>

      <div class="player-stats">
        <div class="row">
          <div class="col-4">
            <div class="games-stats">
              <div class="cirlcle-per-cent">
                50%
              </div>
              <div class="games">
                <p><%= @infos[:total_games] %> games :</p>
                <p><%= @infos[:total_winning_game] %> wins / <%= @infos[:total_loosing_game] %> wins</p>
              </div>
            </div>
          </div>
          <div class="col-4">
            <div class="player-ranking">
              <% if @infos[:tier] == 'CHALLENGER' %>
                <%= image_tag "ranking/Emblem_Challenger.png" %>
              <% elsif @infos[:tier] == 'GRANDMASTER' %>
                <%= image_tag "ranking/Emblem_Grandmaster.png" %>
              <% elsif @infos[:tier] == 'MASTER' %>
                <%= image_tag "ranking/Emblem_Master.png" %>
              <% elsif @infos[:tier] == 'DIAMOND' %>
                <%= image_tag "ranking/Emblem_Diamond.png" %>
              <% elsif @infos[:tier] == 'PLATINIUM' %>
                <%= image_tag "ranking/Emblem_Platinium.png" %>
              <% elsif @infos[:tier] == 'GOLD' %>
                <%= image_tag "ranking/Emblem_Gold.png" %>
              <% elsif @infos[:tier] == 'SILVER' %>
                <%= image_tag "ranking/Emblem_Silver.png" %>
              <% else %>
                <%= image_tag "raking/Emblem_Bronze.png" %>
              <% end %>
              <div class="ranking">
                <p>Rank: <%= @infos[:tier] %> <%= @infos[:rank] %></p>
                <p>League points: <%= @infos[:league_points] %></p>
              </div>
            </div>
          </div>
          <div class="col-4">
            <div class="games-stats">
              <div class="most-played-champions">
                <% most_played_champions = @user.most_champions_played %>
                <% most_played_champions.each do |champion| %>
                  <div class="played-champion">
                    <%= champion['championId'] %>
                  </div>
                <% end %>
              </div>
              <div class="most-used-champ">
                Most used champions
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="stat-category">
        SoloQ history
      </div>

      <div class="history-cards">
        <% matches = @user.five_last_matches_infos_from_riot %>
        <% matches.first(3).each do |match| %>
          <%= render 'shared/match-history', match: match %>
        <% end %>
      </div>
    </div>
  </div>.
</div>

<div class="footer">
  <%= render 'shared/footer' %>
</div>
