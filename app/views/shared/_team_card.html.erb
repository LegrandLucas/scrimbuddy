<% @user.scrims.each do |scrim| %>

  <% if scrim.team_visitor.name == @user.team.name %>
      <div class="card-scrims">
    <% scrim.team_host.images.each do |image|%>
        <%= image_tag image %>

        <div class="card-scrims-infos">
          <h2><%= scrim.team_host.name %></h2>
          <p><%= scrim.result %></p>

        </div>
      </div>

    <% end %>
  <% end %>
<% end %>

